{
  "name": "@lionlang/examples",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build index.ts jev.ts --outdir dist --target bun",
    "dev": "bun run index.ts",
    "jev": "bun --env-file=../typesafe-ai/.env run jev.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lionlang/core": "workspace:*",
    "@lionlang/typesafe-ai": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "catalog:"
  }
}
